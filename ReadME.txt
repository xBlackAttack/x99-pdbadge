You can view all settings on config.lua

Photos are uploaded from the phone. (PlayerData.metadata.phone.profilepicture)

You need to add a shared.lua(qb-core/shared/items.lua) item and make name pdbadge (Settings: useable=true - unique=true)